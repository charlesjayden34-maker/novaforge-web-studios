# Orvanta — Frontend

Vite + React + TypeScript + Tailwind CSS.

## Commands

```bash
npm install
npm run dev
npm run build
```

## Environment

Copy `.env.example` to `.env` and set `VITE_STRIPE_PUBLISHABLE_KEY` for embedded checkout.
